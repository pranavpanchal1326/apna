// src/theme/mapStyle.ts
// Map styles — OpenFreeMap hosted MapLibre styles (free, keyless, OSM data).
// Applied via the `mapStyle` prop on MapLibre's <Map> component.

/** Dark map style — matches app dark theme. */
export const DarkMapStyle = 'https://tiles.openfreemap.org/styles/dark'

/** Light map style — matches app light theme. */
export const LightMapStyle = 'https://tiles.openfreemap.org/styles/positron'
